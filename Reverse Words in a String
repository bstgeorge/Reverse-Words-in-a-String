/**
 * @param {string} s
 * @return {string}
 */
var reverseWords = function(s) {
    let arr = s.trim().split(' ');
    let result = []
    for (let i = 0; i < arr.length; i++) {
      if (arr[i] !== '') {
        result.unshift(arr[i]);
      }
    }
    return result.join(' ')
};
